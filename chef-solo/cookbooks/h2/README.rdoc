= DESCRIPTION:
Install H2 using jar file downloaded from maven repository
= REQUIREMENTS:
Require JRE
= ATTRIBUTES:
##
[:h2][:version] # H2 Jar Version
[:h2][:user] # H2 User 
[:h2][:group] # H2 Group
[:h2][:target] # Prefix

